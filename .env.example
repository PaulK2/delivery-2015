# The backend now runs as a Worker deployed alongside this app (see worker/), reached
# same-origin at /api — nothing to configure here for that.
#
# Local secrets (the password-hashing salt) go in .dev.vars — see .dev.vars.example.
